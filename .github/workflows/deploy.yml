name: Deploy to Bluehost

on:
  push:
    branches:
      - main  # Change if you use a different branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Upload to FTP server
        uses: SamKirkland/FTP-Deploy-Action@4.3.1
        with:
          server: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          local-dir: ./
          server-dir: /home2/thencmeo/public_html/  # Directory on the Bluehost server
          exclude: |
            **/.git*
            **/node_modules/**
            **/yarn.lock
            **/.github/**

      - name: Add or update config.php files on server
        run: |
          echo "Updating config.php files on the server"
          ftp -inv ${{ secrets.FTP_SERVER }} <<EOF
          user ${{ secrets.FTP_USERNAME }} ${{ secrets.FTP_PASSWORD }}
          cd /home2/thencmeo/public_html/config/
          put <(echo "
          <?php
          /*
           * Database Configuration
           */
          define('DB_HOST', '${{ secrets.DB_HOST }}');
          define('DB_USER', '${{ secrets.DB_USER }}');
          define('DB_PASSWORD', '${{ secrets.DB_PASSWORD }}');
          define('DB_DATABASE', '${{ secrets.DB_DATABASE }}');
          ?>
          ") config.php
          
          cd /home2/thencmeo/public_html/admin/config/
          put <(echo "
          <?php
          /*
           * Database Configuration
           */
          define('DB_HOST', '${{ secrets.DB_HOST }}');
          define('DB_USER', '${{ secrets.DB_USER }}');
          define('DB_PASSWORD', '${{ secrets.DB_PASSWORD }}');
          define('DB_DATABASE', '${{ secrets.DB_DATABASE }}');
          ?>
          ") config.php
          bye
          EOF
